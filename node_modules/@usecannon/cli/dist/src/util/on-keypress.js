"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_readline_1 = __importDefault(require("node:readline"));
function onKeypress(handleKeyPress) {
    return new Promise((resolve) => {
        const rl = node_readline_1.default.createInterface({
            input: process.stdin,
            escapeCodeTimeout: 50,
        });
        node_readline_1.default.emitKeypressEvents(process.stdin, rl);
        process.stdin.setRawMode(true);
        process.stdin.resume();
        const listener = (_, key) => {
            handleKeyPress(key, { pause, stop });
        };
        const pause = async (fn) => {
            process.stdin.off('keypress', listener);
            process.stdin.setRawMode(false);
            await fn();
            process.stdin.on('keypress', listener);
            process.stdin.setRawMode(true);
            process.stdin.resume();
        };
        const stop = () => {
            process.stdin.off('keypress', listener);
            process.stdin.setRawMode(false);
            rl.close();
            resolve(null);
        };
        process.stdin.on('keypress', listener);
    });
}
exports.default = onKeypress;
//# sourceMappingURL=on-keypress.js.map