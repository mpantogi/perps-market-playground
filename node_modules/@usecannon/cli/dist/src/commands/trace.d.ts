import * as viem from 'viem';
import { CliSettings } from '../settings';
export declare function trace({ packageRef, data, chainId, cliSettings, from, to, value, block, json, }: {
    packageRef: string;
    data: viem.Hex;
    chainId: number;
    preset: string;
    cliSettings: CliSettings;
    from?: viem.Address;
    to?: viem.Address;
    block?: string;
    value?: bigint;
    json: boolean;
}): Promise<void>;
//# sourceMappingURL=trace.d.ts.map