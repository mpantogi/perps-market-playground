import { CannonRegistry } from '@usecannon/builder';
import { CliSettings } from '../settings';
interface Params {
    packageRef: string;
    cliSettings: CliSettings;
    tags?: string[];
    onChainRegistry: CannonRegistry;
    chainId?: number;
    presetArg?: string;
    quiet?: boolean;
    includeProvisioned?: boolean;
    skipConfirm?: boolean;
}
export declare function publish({ packageRef, cliSettings, onChainRegistry, tags, chainId, presetArg, quiet, includeProvisioned, skipConfirm, }: Params): Promise<void>;
export {};
//# sourceMappingURL=publish.d.ts.map