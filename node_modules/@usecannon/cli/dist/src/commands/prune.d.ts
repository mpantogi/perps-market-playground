import { CannonStorage } from '@usecannon/builder';
export interface PruneStats {
    deletedSize: number;
    matchedFromRegistry: number;
    notExpired: number;
    notCannonPackage: number;
}
export declare function prune(storage: CannonStorage, packageFilters: string[], chainIds: number[], keepAge: number): Promise<[string[], PruneStats]>;
//# sourceMappingURL=prune.d.ts.map